name: Email Step Demo

on:
  workflow_dispatch: # Manual trigger

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f3003127@ds.study.iitm.ac.in
        run: echo "Hello, world! This step name is my email."
